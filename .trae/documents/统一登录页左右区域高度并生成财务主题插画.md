## 目标

* 左侧白色背景区域高度与右侧“用户登录”白色区域完全一致，整体对齐整齐协调。

* 在左侧区域重新生成一张美观且与“财务云记账系统”相关的插画（SVG），体现业务含义：账簿/凭证/报表/趋势图等。

## 样式调整

* 容器对齐

  * 在 `styles.css` 的 `.login-container`（c:\Users\FJ\Documents\trae\_projects\cwxm\styles.css:9）设置 `align-items: stretch`，让两列子元素高度拉伸一致。

  * 将 `grid-template-columns` 保持为 `minmax(0,640px) minmax(0,420px)`，保证左右宽度稳定。

* 左右子卡片拉伸

  * `.login-hero`（styles.css:10）：改为 `height:100%`、`display:flex`、`align-items:center`、`justify-content:center`，移除或下调 `min-height`，使其随父容器高度拉伸；保留统一边框、圆角与阴影。

  * `.login-card`（styles.css:12）：设置 `height:100%`，与左侧保持一致的外观参数（边框、圆角、阴影、内边距），避免视觉高度差。

* 图片适配

  * `.hero-img`（styles.css:11）：设为 `width:100%`、`height:100%`、`object-fit:contain`，在容器内完整展示不裁切；去除固定 `aspect-ratio`，由容器控制比例。

* 响应式

  * 保持现有中小屏隐藏左侧区域（媒体查询）以保证表单可用性；在 >=1060px 视口下展示左右布局。

## 插画生成（/hero.svg）

* 路由：更新 `server.js` 的 `GET /hero.svg`（c:\Users\FJ\Documents\trae\_projects\cwxm\server.js:33）生成新的 SVG：

  * 使用 `viewBox="0 0 800 540"` 与 `preserveAspectRatio="xMidYMid meet"`，在任何容器尺寸下保持比例与居中。

  * 设计元素：

    * 仪表盘卡片：标题“财务云记账 · 总览”，内含柱状图（收入/成本/费用/利润）、饼图（科目占比）、折线图（期间趋势）。

    * 凭证/账簿图标：表示记账与核算业务。

    * 背景格点与渐变：品牌蓝系渐变，轻阴影与柔和边框，统一风格。

  * 色彩与层次：

    * 品牌蓝（#1e78e7/#3ba3ff），辅助紫（#6b5ff9）、绿（#22c55e）、橙（#f59e0b）。

    * 卡片背景 `#fff → #f5faff` 渐变，边框 `#e5e7eb`，阴影轻量。

* 可扩展：支持查询参数（如 `?theme=blue`、`?metric=profit`）以切换配色或侧重点（后续可选）。

## 页面结构

* `index.html` 的图片引用保持：`<img class="hero-img" src="/hero.svg" alt="财务云记账插画" />`（已存在于 login-hero 容器）。

* 无需改动表单结构；只需样式与 SVG 路由更新即可达成目标。

## 验证步骤

* 启动服务后访问 `http://localhost:3000/`：

  * 检查左右白色区域的可视高度一致（使用浏览器 devtools 对比 `.login-hero` 与 `.login-card` 高度）。

  * 缩放浏览器宽度至 >=1060px：左右卡片同步拉伸；在较窄视口隐藏左侧以保障表单。

  * 刷新与切换页面，确认 SVG 在容器内完整显示（不裁切、不拉伸）。

## 交付内容

* 更新 `styles.css`：`.login-container`、`.login-hero`、`.login-card`、`.hero-img` 高度与适配规则。

* 更新 `server.js`：更美观的 `/hero.svg` 生成逻辑（响应式、品牌风格、财务图形）。

* 保留现有结构与功能，不影响登录逻辑。

